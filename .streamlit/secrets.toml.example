# Copy this file to  .streamlit/secrets.toml  and fill in the real passwords.
# NEVER commit the real secrets.toml (it's in .gitignore).

# ---------------------------------------------------------------------------
# CMIS database — READ ONLY. Source of faculty sessions
# (the `upcoming_trainer_utilization_view` view).
# ---------------------------------------------------------------------------
[cmis]
host = "159.65.156.254"
port = 3306
user = "anudip_tekdi"
password = "PUT-CMIS-PASSWORD-HERE"
database = "anudip_cmis17"

# ---------------------------------------------------------------------------
# Hakathon database — READ & WRITE. The app's own 5 tables live here.
# ---------------------------------------------------------------------------
[appdb]
host = "128.199.28.53"
port = 3306
user = "hakathon_user"
password = "PUT-HAKATHON-PASSWORD-HERE"
database = "Anudip_AE_Team"

# ---------------------------------------------------------------------------
# App auth. Demo password shared by all user_roles accounts (until real SSO).
# ---------------------------------------------------------------------------
[auth]
shared_password = "Password123!"
